
<%= form_with model: board , local: true , class: "" do |f|%>
 <div>

<% if board.new_record? %>
<h3>New board</h3>
<% else %>
<h3>edit board</h3>
<% end %>
<p> create Boards Template</p>
</div>
<div>
<%= f.label :name, class:""%>
    <div>
        <%= f.text_field :name, class:""%>
    </div>
</div>

<% link_to "Cancel" , root_path , class: ""%>
<%= f.submit "Create Board" ,class:"btn btn-outline-dark me-2"%>

<% end %>

<%= render "form" , board: @board %>