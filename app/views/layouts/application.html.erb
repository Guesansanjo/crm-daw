<!DOCTYPE html>
<html>
  <head>
    <title>Alpine</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= favicon_link_tag asset_path("alpine-icon.ico") %>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <script>
  if (global === undefined) {
    var global = window;
  }
</script>
  </head>
  <body>
    <% if flash.any?%>
      <%= render "shared/flashes"%>
    <% end%>
    <%= render "layouts/header" if @should_render_header%>
    <%= yield %>
  </body>
</html>
